export type State = {
  playlists: {}[],
  songs: {
    id: string,
    thumbnail: string,
    title: string,
    artistName: string,
    duration: string,
    quality: string,
    playCount: string,
  }[],
};
